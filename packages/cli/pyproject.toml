[project]
name = "canyonos"
version = "1.0.0"
description = "CanyonOS CLI"
requires-python = ">=3.10"
dependencies = [
    "pyfiglet",
    "python-dotenv",
    "pyyaml",
    "rich",
    "ruamel.yaml",
]

[project.scripts]
canyonos = "cli:main"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["canyonos*", "utils*"]

[tool.setuptools]
py-modules = ["cli"]

[tool.setuptools.package-data]
canyonos = ["dashboard.compose.yml"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
release = [
    "nuitka==4.2.1",
]
