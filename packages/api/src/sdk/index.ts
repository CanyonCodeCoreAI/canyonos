/**
 * SDK Entry Point. For the API type used with Eden Treaty, import from @cc-forge/api/client.
 * This file is a placeholder so the package has a default export surface.
 */
export type ForgeApiType = unknown;
